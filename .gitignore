# Logs (regenerated by the launchd service)
*.log

# Node
node_modules/

# OS
.DS_Store

# Secrets never live in the repo — they're in ~/.strava-proxy/auth.json
# (kept outside this folder on purpose). Listed here as a guard in case
# anyone ever copies it in.
auth.json
.env
