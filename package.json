{
  "name": "cycling-coach",
  "private": true,
  "type": "module",
  "description": "Root package for Vercel serverless functions (api/). The local Strava proxy has its own package.json under proxy/.",
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
